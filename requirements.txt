fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# Phase 0 — Fitness Dashboard data sources (additive).
garminconnect==0.3.6
python-dotenv==1.0.1
# Strava uses DIY OAuth via `requests` (no stravalib by spec). Pinned explicitly
# rather than relying on it being transitively present — the real sync needs it.
requests==2.32.3

